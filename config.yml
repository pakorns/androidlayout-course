title: androidlayout
description: Learning android layout in android studio
template:
  repo: androidlayout-template
  name: androidlayout
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
